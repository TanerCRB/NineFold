# Plan

> Template. One entry per task; the identifier is reserved before work starts, as its own commit
> (`../../FrameworkDoc.md`, section 9 — identifiers are a shared resource). A task is checked off
> only at gate 3, by a human, with a link to the test or artifact that proves it.

## <Block name, e.g. "B1 — Accounts and access">

- [ ] **B1-01** — <title: an action, not a noun>.
  *Done when:* <observable condition: a test name, a query result, an artifact>.
  **Out of scope (explicit):** <item — reason — closing condition if the narrowing is temporary>.

- [x] **B1-02** — <title>.
  *Done when:* <condition>.
  **Out of scope (explicit):** none.
  **Done <YYYY-MM-DD>:** <link to the test or artifact — never just a note>.
