# Gaps in <the other repository>

> Template. Explains **why this repository looks the way it does** while the other side hasn't
> delivered something. It is **not** a channel for ordering work — that is always an Issue in the
> repository that has to do it (`../cross-repo-gaps.md`). An entry leaves this file only in the
> third state.

## <Short name of the gap>

**State:** reported | delivered | closed on our side
**Reported:** [<repo>#<N>](https://github.com/<owner>/<repo>/issues/<N>)
<!-- or: **Reported:** no — <reason and until when>. Silence is not allowed. -->

**What is missing:** <in contract terms — endpoint, field, error code>
**Our workaround:** <what we do instead, with file and lines>
**What we remove once it exists:** <the workaround above, concretely>
